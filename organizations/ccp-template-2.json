{
    "name": "test-network-${ORG}",
    "version": "1.0.0",
    "client": {
        "organization": "${ORG}",
        "connection": {
            "timeout": {
                "peer": {
                    "endorser": "300"
                }
            }
        }
    },
    "organizations": {
        "${ORG}": {
            "mspid": "${ORG}MSP",
            "peers": [
                "peer0.${ORG}.mynetwork.com"
            ],
            "certificateAuthorities": [
                "ca.${ORG}.mynetwork.com"
            ]
        }
    },
    "peers": {
        "peer0.${ORG}.mynetwork.com": {
            "url": "grpcs://localhost:${P0PORT}",
            "tlsCACerts": {
                "pem": "${PEERPEM}"
            },
            "grpcOptions": {
                "ssl-target-name-override": "peer0.${ORG}.mynetwork.com",
                "hostnameOverride": "peer0.${ORG}.mynetwork.com"
            }
        },
        "peer1.${ORG}.mynetwork.com": {
            "url": "grpcs://localhost:${P0PORT}",
            "tlsCACerts": {
                "pem": "${PEERPEM}"
            },
            "grpcOptions": {
                "ssl-target-name-override": "peer1.${ORG}.mynetwork.com",
                "hostnameOverride": "peer1.${ORG}.mynetwork.com"
            }
        }
    },
    "certificateAuthorities": {
        "ca.${ORG}.mynetwork.com": {
            "url": "https://localhost:${CAPORT}",
            "caName": "ca-${ORG}",
            "tlsCACerts": {
                "pem": ["${CAPEM}"]
            },
            "httpOptions": {
                "verify": false
            }
        }
    }
}
